services:
  app:
    build: .
    restart: always
    labels:
      - traefik.http.routers.${TRAEFIK_ROUTE}.rule=Host(${TRAEFIK_APP_DOMAIN})
      - traefik.http.routers.${TRAEFIK_ROUTE}.tls=true
      - traefik.http.routers.${TRAEFIK_ROUTE}.tls.certresolver=lets-encrypt
      - traefik.port=${TRAEFIK_APP_PORT}
    env_file:
      - .env
    networks:
      - web
    expose:
      - "8000"
    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs
      - ./geoip:/app/geoip
    # add memory and cpu limits al compose de docker compose maximo 1cpu y 4 gb de ram
    deploy:
      resources:
        limits:
          cpus: '1.3'
          memory: 5G
networks:
  web:
    external: true
